// playerData.ts
// Provides functions for retrieving and managing player-specific data, such as stats or linked accounts.
export default interface PlayerData {
    Ranks : {
        [key: string]: number;
    }

}